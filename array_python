from array import *
arr=array('i',[])
n=int(input('enter the length of array)
for i in n:
  x=int(input('enter the next value: ')
  arr.append(x)
print(arr)
val=int('input enter a vlaue to search')
k=0
for m in arr:
  if m==val:
  print(k)
  break
  k+=1
  
  
  
